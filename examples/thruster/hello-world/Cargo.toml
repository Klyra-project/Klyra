[package]
name = "hello-world"
version = "0.1.0"
edition = "2021"

[lib]

[dependencies]
klyra-service = { version = "0.6.0", features = ["web-thruster"] }
thruster = { version = "1.2.6", features = ["hyper_server"] }
