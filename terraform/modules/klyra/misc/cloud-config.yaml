#cloud-config

# Install needed packages
packages:
  - docker.io
  - nfs-common

# Create the docker group
groups:
  - docker

# Add default auto-created user to docker group
system_info:
  default_user:
    groups: [docker]

# Make sure files are written every time
cloud_final_modules:
 - package-update-upgrade-install
 - [runcmd, always]
 - [write-files, always]
 - fan
 - landscape
 - lxd
 - ubuntu-drivers
 - write-files-deferred
 - puppet
 - chef
 - mcollective
 - salt-minion
 - reset_rmc
 - refresh_rmc_and_interface
 - rightscale_userdata
 - scripts-vendor
 - scripts-per-once
 - scripts-per-boot
 - scripts-per-instance
 - [scripts-user, always]
 - ssh-authkey-fingerprints
 - keys-to-console
 - install-hotplug
 - phone-home
 - final-message
 - power-state-change

# Create our systemd files
write_files:
  - encoding: b64
    content: ${opt_klyra_content}
    path: /lib/systemd/system/opt-klyra.mount
    owner: root:root
    permissions: "0644"
  - encoding: b64
    content: ${klyra_backend_content}
    path: /lib/systemd/system/klyra-backend.service
    owner: root:root
    permissions: "0644"
  - encoding: b64
    content: ${klyra_provisioner_content}
    path: /lib/systemd/system/klyra-provisioner.service
    owner: root:root
    permissions: "0644"
  - encoding: b64
    content: ${klyra_pg_content}
    path: /lib/systemd/system/klyra-pg.service
    owner: root:root
    permissions: "0644"
  - encoding: b64
    content: ${klyra_mongodb_content}
    path: /lib/systemd/system/klyra-mongodb.service
    owner: root:root
    permissions: "0644"

power_state:
  mode: reboot

# Up services on every boot
runcmd:
  - docker network inspect klyra-net || docker network create --driver bridge klyra-net
  - [systemctl, daemon-reload]
  - [systemctl, enable, "opt-klyra.mount"]
  - [systemctl, enable, "klyra-pg.service"]
  - [systemctl, enable, "klyra-mongodb.service"]
  - [systemctl, enable, "klyra-provisioner.service"]
  - [systemctl, enable, "klyra-backend.service"]
