[package]
name = "sqlx-pool"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[workspace]

[dependencies]
klyra-service = { path = "../../../", features = ["codegen"] }
klyra-shared-db = { path = "../../../../resources/shared-db", features = ["postgres"] }
sqlx = { version = "0.6.2", features = [ "runtime-tokio-native-tls" ] }
